<UserControl x:Class="PubgMod.Views.BottomSheetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:s="https://github.com/canton7/Stylet"
             mc:Ignorable="d"
             d:DesignHeight="65.466" d:DesignWidth="433">
    <Grid>
        <materialDesign:Snackbar IsActive="{Binding IsDisplayed}" MaxWidth="600">
            <materialDesign:SnackbarMessage Content="{Binding Message}" ActionContent="{Binding ActionContent}" ActionCommand="{s:Action CloseBottomSheet}" />
        </materialDesign:Snackbar>
    </Grid>
</UserControl>
